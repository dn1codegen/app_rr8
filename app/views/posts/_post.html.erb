<div id="<%= dom_id post %>" class="card">
  <div class="card-header">
    <h2><%= post.title %></h2>
  </div>
  <div class="card-body">
    <div class="post-content">
      <%= simple_format(post.body) %>
    </div>
  </div>
</div>
